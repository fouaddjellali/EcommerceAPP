{% extends 'base.html.twig' %}

{% block title %}Modifier un Commentaire{% endblock %}

{% block body %}
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Modifier un Commentaire</h1>
            </div>
            <div class="col-sm-6 text-right">
                <a href="{{ path('app_comment_index') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
</div>

<div class="content">
    <div class="container-fluid">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h3 class="card-title"><i class="fas fa-edit"></i> Formulaire de Modification</h3>
            </div>
            <div class="card-body">
                {{ include('comment/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
            <div class="card-footer text-right">
                <a href="{{ path('app_comment_index') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
                {{ include('comment/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}

