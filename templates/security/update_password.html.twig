{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card mt-5">
                <div class="card-header text-center bg-primary text-white">
                    <h4>Mettre à jour votre mot de passe</h4>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        {{ form_row(form.old_password, {'attr': {'class': 'form-control'}}) }}
                        {{ form_row(form.new_password, {'attr': {'class': 'form-control'}}) }}
                        {{ form_row(form.submit, {'attr': {'class': 'btn btn-primary w-100'}}) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}